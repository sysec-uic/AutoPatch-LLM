# this file is intended for interactive development only

services:
  autopatch:
    build: ./src
    container_name: autopatch
    env_file:
      - ./src/.env
    volumes:
      - ./afl-2.52b:/app/afl-2.52b
      - ./src/bugLog:/app/bugLog
      - ./src/codebase:/app/codebase
      - ./src/executables:/app/executables
      - ./src/executables_afl:/app/executables_afl
      - ./src/input:/app/input
      - ./src/patched_codes:/app/patched_codes
    working_dir: /app

